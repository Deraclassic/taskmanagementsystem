server:
  port: 5002

spring:
  application:
    name: TASK-SERVICE

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/yt_task_service
    username: root
    password: 08033468393dad
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  devtools:
    restart:
      enabled: false

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
